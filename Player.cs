using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player : Actor
{
    // Player 클래스는 자식클래스
    // Actor 클래스는 부모클래스
    // 상속
    // 자식클래스는 부모클래스의 모든 멤버변수와 함수를 사용할수있음
    // 자기자신의 새로운 멤버변수, 멤버함수 추가가능
}
